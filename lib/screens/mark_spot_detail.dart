import 'package:flutter/material.dart';

class MarkSpotDetailScreen extends StatelessWidget {
    const MarkSpotDetailScreen({super.key});

    @override
    Widget build(BuildContext context) {
      return Scaffold(
        body: const Center(child: Text('Mark spot detail')),
      );
    }
  }
